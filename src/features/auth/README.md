# Auth

Responsabilité: porter l'authentification applicative, les sessions et les futurs flux Better Auth.

Frontières prévues:
- `services/` pour les cas d'usage auth
- `policies/` pour les décisions d'accès partagées
- `data/` pour les accès persistance spécifiques
- `components/` pour les composants UI liés au domaine
